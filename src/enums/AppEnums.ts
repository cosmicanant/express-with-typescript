export enum MetadataKeys {
  method = 'method',
  path = 'path',
  middlewares = 'middlewares'
}

export enum Methods {
  get = 'get',
  put = 'put',
  post = 'post',
  del = 'delete',
  patch = 'patch'
}
